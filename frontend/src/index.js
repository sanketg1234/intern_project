import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById("root")); // Define 'root' correctly
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);